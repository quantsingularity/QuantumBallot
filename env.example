# ----------------------------------------------------------------
# QuantumBallot Environment Variables Example
# ----------------------------------------------------------------

# This file contains all environment variables used by the QuantumBallot project.
# Copy this file to .env and fill in your desired values.

# ----------------------------------------------------------------
# 1. Backend API Configuration (Used by backend-api)
# ----------------------------------------------------------------

# Server Port
PORT=3000

# Node Environment (e.g., development, production)
NODE_ENV=development

# JWT Secrets (MUST be strong, random strings)
ACCESS_TOKEN_SECRET=your_access_token_secret
REFRESH_TOKEN_SECRET=your_refresh_token_secret

# Encryption Keys and IVs for Sensitive Data (MUST be strong, random strings)
# Used for encrypting identifiers and votes in the smart contract interaction layer.
SECRET_KEY_IDENTIFIER=your_32_byte_hex_key_for_identifier
SECRET_IV_IDENTIFIER=your_16_byte_hex_iv_for_identifier
SECRET_KEY_VOTES=your_32_byte_hex_key_for_votes
SECRET_IV_VOTES=your_16_byte_hex_iv_for_votes

# Email Configuration (for notifications/verification)
MAILER_SERVICE=gmail
MAILER_HOST=smtp.gmail.com
MAILER_PORT=587
MAILER_USER=your_email_address
MAILER_PASS=your_email_password_or_app_password

# ----------------------------------------------------------------
# 2. Frontend Configuration (Used by web-frontend)
# ----------------------------------------------------------------

# API URL for the web-frontend to connect to the backend API.
# Since the backend runs on port 3000, this should point there.
VITE_API_URL=http://localhost:3000
